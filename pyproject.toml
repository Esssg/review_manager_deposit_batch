[project]
name = "bank-account-deposit-batch"
version = "0.1.0"
description = "Fetch bank transactions from BankAPI and insert them into Supabase."
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.0.0",
    "requests>=2.32.0",
    "supabase>=2.18.0",
]

[tool.uv]
package = false
